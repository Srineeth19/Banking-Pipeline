name: 'banking_dbt'
version: '1.0.0'
config-version: 2

profile: 'banking_dbt'

model-paths: ["models"]
snapshot-paths: ["snapshots"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  banking_dbt:
    staging:
      +materialized: view
      +schema: silver
    marts:
      +materialized: table
      +schema: gold
      transactions:
        +materialized: incremental
        +incremental_strategy: merge
